<div class="prose">
    <slot />
</div>

<style is:global lang="scss">
    .prose {
        font-size: 1rem;
        line-height: 1.8;
        color: var(--fg);

        p {
            font-family: var(--font-prose);
            margin-bottom: var(--space-6);
        }

        h2,
        h3,
        h4 {
            color: var(--fg-strong);
            transition: text-shadow var(--duration-fast);

            > a {
                color: inherit;
                text-decoration: none;
            }
        }

        h2:hover,
        h3:hover,
        h4:hover,
        h2:hover > a,
        h3:hover > a,
        h4:hover > a {
            text-shadow: var(--shadow-offset-hover) var(--shadow-offset-hover) 0 var(--accent);
        }

        h2 {
            font-family: var(--font-display);
            font-size: clamp(1.5rem, 4vw, 2rem);
            letter-spacing: 0.02em;
            margin: 2.5rem 0 var(--space-4);
            border-bottom: var(--border-width-lg) solid var(--border);
            padding-bottom: 0.5rem;
        }

        h3 {
            font-family: var(--font-display);
            font-size: 1.2rem;
            letter-spacing: 0.02em;
            margin: var(--space-8) 0 0.75rem;
        }

        h4 {
            font-weight: 700;
            font-size: 1rem;
            margin: var(--space-6) 0 var(--space-2);
        }

        a {
            color: var(--fg-strong);
            text-decoration: underline solid var(--fg-strong);
            text-underline-offset: 3px;
            text-shadow: none;
            transition: text-shadow var(--duration-fast);

            &:hover,
            &:focus-visible {
                text-shadow: var(--shadow-offset) var(--shadow-offset) 0 var(--accent);
            }
        }

        h5 > a,
        h6 > a {
            color: inherit;
            text-decoration: none;
        }

        strong {
            color: var(--fg-strong);
            font-weight: 700;
        }

        em {
            color: var(--fg-strong);
            font-style: italic;
        }

        ul,
        ol {
            padding-left: var(--space-6);
            margin-bottom: var(--space-6);
        }

        li {
            font-family: var(--font-prose);
            margin-bottom: 0.35rem;

            > ul,
            > ol {
                margin-top: 0.35rem;
                margin-bottom: 0;
            }
        }

        blockquote {
            border-left: var(--border-width-lg) solid var(--border);
            padding-left: 1.25rem;
            margin: var(--space-6) 0;
            color: var(--fg);
            font-style: italic;
        }

        hr {
            border: none;
            border-top: var(--border-width-lg) solid var(--border);
            margin: 2.5rem 0;
        }

        img {
            width: 100%;
            height: auto;
            border: var(--border-width) solid var(--border);
            box-shadow: var(--shadow-stamp);
            margin: var(--space-6) 0;
        }

        :not(pre) > code {
            font-family: var(--font-mono);
            font-size: 0.88em;
            padding: 0.15em 0.4em;
            background: var(--surface);
            border: var(--border-width) solid var(--border);
            color: var(--fg-strong);
        }

        figure[data-rehype-pretty-code-figure] {
            margin: var(--space-6) 0;
            border: var(--border-width) solid var(--border);
            box-shadow: var(--shadow-stamp);
            position: relative;

            &:has([data-rehype-pretty-code-title]) {
                margin-top: calc(var(--space-6) + 1.75rem);
            }
        }

        [data-rehype-pretty-code-title] {
            position: absolute;
            bottom: 100%;
            margin-bottom: calc(-1 * var(--border-width));
            left: calc(-1 * var(--border-width));
            display: inline-block;
            font-family: var(--font-mono);
            font-size: 0.72rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.06em;
            color: var(--fg-strong);
            background: var(--surface);
            border: var(--border-width) solid var(--border);
            border-bottom: none;
            padding: 0.25em 0.75em;
        }

        pre {
            font-family: var(--font-mono);
            font-size: 0.88rem;
            overflow-x: auto;
            background: var(--surface);
            margin: 0;

            code {
                font-family: var(--font-mono);
                padding: 1.25rem;
                display: block;
                background: transparent;
            }
        }

        [data-highlighted-line] {
            background: rgba(255, 255, 255, 0.04);
        }

        [data-line-numbers] {
            counter-reset: line;

            [data-line]::before {
                counter-increment: line;
                content: counter(line);
                display: inline-block;
                width: 1.5rem;
                margin-right: var(--space-6);
                color: var(--fg);
                text-align: right;
                font-size: 0.8em;
            }
        }

        [data-language="diagram"] {
            line-height: 1.25em;
        }

        [data-language="sprite"] {
            line-height: 0.6em;
        }

        button.rehype-pretty-copy {
            background: none;
            border: none;
            cursor: pointer;
            top: 0.5rem;
            right: 0.5rem;
            margin: 0;
            transition: filter var(--duration-fast);

            span {
                filter: brightness(1.8);
            }

            &:hover span {
                filter: brightness(2.2);
            }
        }
    }
</style>
